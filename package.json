{
  "name": "discord-notificator",
  "packageManager": "yarn@4.0.0",
  "scripts": {
    "build": "cp appsscript.json dist/ && node build.js"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.77",
    "esbuild": "^0.19.6",
    "esbuild-gas-plugin": "^0.8.0"
  }
}
